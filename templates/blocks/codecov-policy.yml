coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 0%
        base: auto
    patch:
      default:
        target: 90%
        threshold: 0%
        base: auto

comment:
  layout: "reach, diff, files"
  require_changes: true
